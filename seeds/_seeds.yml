seeds:
  - name: employees
    description: blablabla
    config:
      grants:
        select: ['accountadmin', 'public']

    columns:
      - name: employee_id
        description: unique key
        data_tests:
          - unique
          - not_null
